<%= form_for(@book_shelf) do |f| %>
  <% if @book_shelf.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@book_shelf.errors.count, "error") %> prohibited this book_shelf from being saved:</h2>

      <ul>
      <% @book_shelf.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user %><br />
    <%= f.text_field :user %>
  </div>
  <div class="field">
    <%= f.label :book %><br />
    <%= f.text_field :book %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
